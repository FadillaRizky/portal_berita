import 'package:flutter/material.dart';

class ScreenMode extends StatelessWidget {
  const ScreenMode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
